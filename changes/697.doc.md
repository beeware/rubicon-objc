Add docstring for `NSEdgeInsetsMake` and fix doc cross-references.

- Add a short docstring to `rubicon.objc.types.NSEdgeInsetsMake` so it appears in
  generated API docs.
- Replace Sphinx `:func:` role references in `src/rubicon/objc/types.py` with
  explicit mkdocstrings-compatible links to ensure cross-references render.

